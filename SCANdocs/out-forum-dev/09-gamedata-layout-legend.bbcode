[INDENT][SUP]*[/SUP] required for SCANsat in IVA glass cockpit, but otherwise   optional. SCANsatRPM will not be loaded if it is not needed, so it is   safe to install in either case.

[SUP]**[/SUP] required for MechJeb in IVA glass cockpit. Currently, SCANsat does not interact with MechJeb but this may change in the future.

[SUP]†[/SUP] optional; required for adding SCANsat functionality to existing parts/pods/vessels/etc.
[/INDENT]
 
